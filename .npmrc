# eslint-plugin-react and eslint-plugin-react-hooks have not yet declared
# ESLint v10 in their peer dependency ranges. The @eslint/compat wrappers
# in .github/linters/eslint.config.mjs handle the actual runtime compatibility.
# This flag can be removed once those plugins publish ESLint v10 peer dep support.
legacy-peer-deps=true
